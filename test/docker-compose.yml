services:
  run-ts-test:
    build:
      context: .
      dockerfile_inline: |
        FROM node:22
        RUN npm install -g typescript
    working_dir: /work
    volumes:
      - ../dist/run-ts:/usr/local/bin/run-ts:ro
